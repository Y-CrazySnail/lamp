<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.snail.chinaybop.mapper.PermissionMapper">
    <select id="permissionListByUserId" resultType="com.snail.chinaybop.entity.Permission">
        select sp.* from sys_user_role sur
        left join sys_role_permission srp on sur.role_id = srp.role_id
        left join sys_permission sp on srp.permission_id = sp.id
        where sur.user_id = #{userId}
        order by id
    </select>
</mapper>