<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yeem.zero.mapper.ZeroAddressMapper">
    <select id="listByUserId" resultType="com.yeem.zero.entity.ZeroAddress">
        SELECT
            za.id,
            za.user_id,
            za.name,
            za.phone,
            za.province,
            za.city,
            za.county,
            za.address,
            za.label,
            za.default_flag
        FROM zero_address za
        WHERE za.delete_flag = 0 and za.user_id = #{userId}
    </select>
</mapper>