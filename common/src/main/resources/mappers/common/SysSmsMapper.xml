<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yeem.common.im.mapper.SysSmsMapper">
    <select id="getTodo" resultType="com.yeem.common.im.entity.SysSMS">
        SELECT *FROM sys_sms WHERE (timing_flag = 1 and send_flag &lt;=1 and timing_time &lt;=NOW()and(state = 0 or state = 9))or( timing_flag
        = 0 and send_flag &lt;=1
        and(state = 0 or state = 9))
    </select>
</mapper>